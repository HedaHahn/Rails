<h1>Posts#show</h1>

<h1><%= @question.title %></h1>

  <div class="row">
    <div class="col-md-8">
      <p><%= @question.body %></p>
    </div>

    <h1><%= @question.title %></h1>
    <p><%= @question.body %></p>
    <p><%= @question.resolved %></p>

   <div class="col-md-4">
     <%= link_to "Edit", edit_question_path(@post), class: 'btn btn-success' %>
     <%= link_to "Delete Question", @pquestion, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this question?'} %>
   </div>
 </div>
